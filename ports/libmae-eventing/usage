The package librsvg provides CMake targets:

    find_package(mae_eventing 0.10.7 CONFIG)
    //or: find_package(mae_eventing CONFIG)

    include_directories(${MAE_EVENTING_INCLUDE_DIRS})
    target_link_libraries(main PRIVATE ${MAE_EVENTING_LIBRARY})
